[project]
name = "taknn"
version = "0.1.0"
dependencies = [
    "torch>=2.1",
    "numpy>=1.24",
    "onnx>=1.14",
    "onnxruntime>=1.16",
    "zstandard>=0.21",
    "tqdm",
    "pyyaml",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
